[tool.poetry]
name = "scanuctenek"
version = "0.1.0"
description = "Nástroj pro skenování a zpracování účtenek"
authors = ["Váš Autor <email@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"
black = "^22.0"
flake8 = "^4.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.urls]
repository = "https://github.com/yourusername/scanuctenek"